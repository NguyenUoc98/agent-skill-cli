# Test Registry — [Project Name]

> QA context store for the Tester Agent. Read this at the start of every testing session.
> This file is maintained by the QA team and updated after each sprint.

## Quick Reference

| Resource | Path | Owner |
|---|---|---|
| Environment Config | `test-registry/environment.md` | QA Team |
| Feature Coverage Map | `test-registry/feature-map.md` | QA Agent (auto-updated) |
| Execution Runs Archive | `test-registry/runs/{JIRA-KEY}/` | QA Agent (auto-written) |
| Test Spec Artifacts | `openspec/changes/test-{JIRA-KEY}/` | QA Agent (archived after sprint) |

## How to Start Testing

1. Fill in `test-registry/environment.md` with the project's DEV/STG URLs.
2. Run `/tester-analyze <JIRA-KEY>` to start Phase 1 for a new ticket.

## Active Sprint

> [Update this section at the start of each sprint]

- Sprint: —
- Period: —
- Focus areas: —
