# Execution Runs

This directory stores test execution artifacts per Jira ticket.
Created automatically by the Tester Agent during `/tester-execute`.

## Structure

```
runs/
└── {JIRA-KEY}/
    ├── execution-log.md    ← Per-case pass/fail records + Playwright evidence
    └── summary-report.md   ← Sprint closure QA report
```
